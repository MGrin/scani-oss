{
  "name": "@scani/deadline",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "description": "A bound on a single await of a remote dependency. One helper, no dependencies — so any package may take it without inheriting anything else.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "type-check": "tsgo --noEmit -p tsconfig.test.json"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
