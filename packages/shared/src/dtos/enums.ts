export type AccountType = {
  name: string;
  description: string | null;
  code: string;
  id: string;
  displayOrder: number;
};

export type InstitutionType = {
  name: string;
  description: string | null;
  code: string;
  id: string;
  displayOrder: number;
};
