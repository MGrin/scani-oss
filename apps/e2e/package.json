{
  "name": "@scani/e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "test:e2e": "bun scripts/run.ts",
    "test:e2e:ui": "bun scripts/run.ts --ui",
    "test:e2e:install": "bunx playwright install --with-deps chromium webkit",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "type-check": "tsgo --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/bun": "latest"
  }
}
