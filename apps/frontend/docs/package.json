{
  "name": "@scani/docs",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "bun scripts/generate-llms-txt.ts && astro build && bun scripts/check-tables.ts && bun scripts/check-robots.ts",
    "preview": "astro preview",
    "clean": "rm -rf dist .astro",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "type-check": "astro check && tsgo --noEmit -p tsconfig.test.json"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "@astrojs/markdown-remark": "^7.2.2",
    "@astrojs/starlight": "^0.40.0",
    "astro": "^6.3.1",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
